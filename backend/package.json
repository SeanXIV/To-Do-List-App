{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building...'",
    "vercel-build": "npm run build && echo 'Vercel build...'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "backend": "file:",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "mongodb": "^6.10.0",
    "mongoose": "^8.8.0"
  }
}
